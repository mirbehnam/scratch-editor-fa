# گزارش تغییرات شاخه `my-develop`

این سند تفاوت‌های مهم شاخه `my-develop` با شاخه `develop` پروژه Scratch Editor و روش ساخت خروجی مناسب اندروید و ویندوز را توضیح می‌دهد. هدف این شاخه، ارائه نسخه فارسی، سریع و کاملاً آفلاین Scratch Editor برای اجرا در Android WebView است، در حالی که خروجی در محیط ویندوز نیز قابل استفاده باقی می‌ماند.

## نسخه مبنا

این شاخه با تغییرات Scratch Editor نسخه `15.1.0` تا کامیت `98ffed8a2` از شاخه `origin/develop` همگام شده است. از معماری رسمی جدید آدرس تصاویر کتابخانه (`GUIStorage.getLibraryAssetUrl`) استفاده می‌شود، اما پیاده‌سازی `LegacyStorage` این آدرس‌ها را به فایل‌های محلی `static/assets` هدایت می‌کند تا سرعت و اجرای کاملاً آفلاین Android حفظ شود.

## خلاصه تغییرات

- افزودن رابط ارتباطی جاوااسکریپت با برنامه اندروید برای بازکردن و ذخیره پروژه‌های `.sb3` و اسپرایت‌های `.sprite3`.
- دانلود و قرار دادن دارایی‌های کتابخانه Scratch در خود پروژه برای اجرای کاملاً آفلاین.
- افزودن بیلد بهینه‌شده مخصوص Android/WebView با مسیرهای نسبی، فشرده‌سازی و بدون source map.
- فارسی‌سازی رابط و استفاده از فونت BYekan، با فارسی و راست‌به‌چپ به‌عنوان حالت پیش‌فرض.
- ترجمه نام اسپرایت‌ها، پس‌زمینه‌ها و صداهای پیش‌فرض و جست‌وجوی دوزبانه فارسی/انگلیسی.
- سریع‌ترشدن نمایش تصویر بندانگشتی کتابخانه در Android با استفاده مستقیم از فایل‌های محلی.
- محدودکردن زبان‌های قابل انتخاب به فارسی، عربی و انگلیسی.
- اصلاح جهت عملگرهای کوچک‌تر و بزرگ‌تر در فارسی و عربی.
- ترجمه عبارت «Load from your computer» در فارسی و عربی.
- اصلاح ورودی‌های عددی در Android: صفحه‌کلید عددی داخلی Scratch باز نمی‌شود و صفحه‌کلید عددی سیستم استفاده می‌شود.
- اصلاح بازنشدن زیرمنوی انتخاب زبان و تنظیمات در دستگاه‌های لمسی Android.
- حفظ رفتار معمول نسخه دسکتاپ و ویندوز برای بخش‌هایی که مخصوص Android هستند.

## تاریخچه کامیت‌های مهاجرت

| کامیت | موضوع |
| --- | --- |
| `a102e6265` | افزودن Android project bridge برای بارگذاری و ذخیره فایل‌ها |
| `f35f949da` | قراردادن دارایی‌های کتابخانه در پروژه برای اجرای آفلاین |
| `48cbda0b1` | افزودن بیلد فارسی و بهینه‌شده Android |
| `ee65c746c` | بارگذاری مستقیم تصاویر کتابخانه برای رفع کندی Android |
| `b7fae2f36` | فارسی‌سازی نام‌های کتابخانه و جست‌وجوی دوزبانه |

تغییرات تکمیلی پس از این کامیت‌ها نیز در بخش‌های بعدی این سند آمده‌اند.

## جزئیات قابلیت‌ها

### ۱. ارتباط با برنامه Android

برای بارگذاری پروژه از برنامه میزبان، این توابع سراسری در صفحه در دسترس هستند:

```javascript
AndroidScratchLoadProject()
AndroidScratchLoadProjectUrl(url, filename)
```

رابط جاوااسکریپت مورد انتظار در WebView با نام `AndroidProjectSaver` ثبت می‌شود. برنامه اندروید می‌تواند این callbackهای اختیاری را نیز دریافت کند:

```text
projectLoaderReady()
projectLoadStarted()
```

ذخیره پروژه به‌صورت تکه‌ای انجام می‌شود:

```text
beginSave(filename)
appendChunk(base64Chunk)
finishSave()
```

ذخیره اسپرایت نیز رابط جداگانه دارد:

```text
beginSpriteSave(filename, size)
appendSpriteChunk(base64Chunk)
finishSpriteSave()
```

اندازه هر تکه Base64 برابر ۲۵۶ KiB است تا انتقال فایل‌های بزرگ از WebView پایدارتر باشد. اگر رابط اندروید وجود نداشته باشد، ذخیره معمول مرورگر به‌عنوان fallback باقی می‌ماند.

### ۲. اجرای کاملاً آفلاین

دارایی‌های اسپرایت، لباس، پس‌زمینه و صدا در مسیر `packages/scratch-gui/static/assets/` نگهداری می‌شوند. اسکریپت دانلود دارایی‌ها، آدرس‌دهی فایل‌ها و Scratch Storage طوری تنظیم شده‌اند که بیلد نهایی برای نمایش کتابخانه به اینترنت وابسته نباشد.

در این شاخه ۱۳۵۶ فایل در پوشه دارایی‌های محلی قرار گرفته است. ابزار بررسی بیلد، وجود ۱۳۴۷ دارایی ارجاع‌شده کتابخانه را کنترل می‌کند. فهرست تک‌تک فایل‌های تولیدشده در این سند تکرار نشده است؛ خود پوشه `static/assets` مرجع کامل آن‌هاست.

### ۳. بیلد بهینه Android و Windows

بیلد Android ویژگی‌های زیر را دارد:

- حالت production و کد فشرده‌شده؛
- مسیرهای نسبی برای استفاده داخل WebView؛
- تنها صفحه اصلی مورد نیاز برنامه؛
- حذف source mapها و صفحات آزمایشی اضافی از خروجی؛
- کپی فونت، extensionها و دارایی‌های آفلاین لازم؛
- پاک‌سازی خروجی قبلی پیش از ساخت.

این خروجی محدود به Android API نیست و می‌تواند در ویندوز نیز از طریق یک وب‌سرور محلی یا WebView دسکتاپ اجرا شود. استفاده مستقیم با `file://` برای همه مرورگرها تضمین نمی‌شود؛ روش مطمئن، سروکردن پوشه با یک سرور محلی یا asset loader است.

### ۴. فارسی‌سازی رابط کاربری

- زبان پیش‌فرض فارسی و جهت پیش‌فرض راست‌به‌چپ است.
- فونت BYekan به رابط فارسی و عنوان دسته‌های بلوک‌ها اعمال شده است.
- بخش‌های غیرضروری نسخه میزبان مانند placeholderهای آموزشی/آزمایشی از خروجی Android کنار گذاشته شده‌اند.
- لوگو و تنظیمات رابط برای استفاده در برنامه Android سازگار شده‌اند.
- عبارت بارگذاری فایل از رایانه برای فارسی و عربی ترجمه شده است.

### ۵. کتابخانه فارسی و جست‌وجوی دوزبانه

نام‌های پیش‌فرض اسپرایت‌ها، پس‌زمینه‌ها و صداها هنگام انتخاب زبان فارسی، فارسی نمایش داده می‌شوند. نام فایل‌ها یا دارایی‌های سفارشی کاربر تغییر نمی‌کند.

در حالت فارسی، جست‌وجو با نام فارسی یا انگلیسی نتیجه یکسان را پیدا می‌کند. جست‌وجو موارد زیر را نیز یکسان‌سازی می‌کند:

- تفاوت حروف عربی و فارسی مانند `ي/ی` و `ك/ک`؛
- اعراب و نشانه‌های نامرئی متن؛
- ارقام فارسی و انگلیسی؛
- فاصله‌ها و شکل‌های رایج نوشتاری.

نام `Kiran` به دلیل احتمال برداشت نامناسب از ترجمه قبلی، به‌صورت «کایرن» نمایش داده می‌شود.

### ۶. سرعت نمایش تصاویر کتابخانه

در Android، تبدیل هر تصویر محلی به Blob باعث تأخیر محسوس و تقریباً ترتیبی در نمایش thumbnailها می‌شد. تصاویر کتابخانه اکنون مستقیماً از URL محلی بیلد خوانده می‌شوند تا فهرست اسپرایت‌ها و پس‌زمینه‌ها سریع نمایش داده شود.

### ۷. زبان‌های قابل انتخاب

فقط سه زبان زیر در منوی زبان و bundle ترجمه‌ها قرار دارند:

- فارسی (`fa`)
- عربی (`ar`)
- انگلیسی (`en`)

localeهای پشتیبانی‌نشده پذیرفته نمی‌شوند. حذف ترجمه‌های دیگر مقداری از حجم JavaScript نهایی کم می‌کند، اما بیشترین سهم حجم خروجی همچنان متعلق به رسانه‌ها، موتور Scratch و وابستگی‌های آن است.

### ۸. جهت عملگرهای مقایسه‌ای

چیدمان ورودی‌های بلوک در زبان راست‌به‌چپ با نمایش بصری علامت مقایسه یکسان نیست. متن بلوک‌های `<` و `>` برای فارسی و عربی به‌صورت مستقل تنظیم شده تا معنای کوچک‌تر و بزرگ‌تر درست باقی بماند. عملگرهای جمع، تفریق و تقسیم تغییری نکرده‌اند.

### ۹. ورودی عددی در Android

در دستگاه Android، کلیک یا لمس ورودی عددی دیگر صفحه‌کلید عددی داخلی Scratch را باز نمی‌کند و صفحه‌کلید عددی سیستم‌عامل استفاده می‌شود. تشخیص ورودی به `pointerType` خاصی وابسته نیست تا در WebViewهای مختلف درست کار کند. این رفتار برای ویندوز فعال نمی‌شود.

ورودی شناور بالای صفحه‌کلید در نسخه وب وجود ندارد، زیرا این قابلیت در خود برنامه Android پیاده‌سازی شده است.

### ۱۰. منوهای لمسی

زیرمنوی زبان و تنظیمات در نمایشگرهای لمسی ممکن بود خارج از محدوده قابل مشاهده باز شود. در دستگاه‌های بدون hover، زیرمنو داخل عرض منوی اصلی قرار می‌گیرد؛ رفتار hover و چیدمان دسکتاپ بدون تغییر باقی مانده است.

## نصب وابستگی‌ها

فرمان‌ها را از ریشه مخزن اجرا کنید:

```powershell
npm ci
```

`npm ci` دقیقاً از نسخه‌های ثبت‌شده در `package-lock.json` استفاده می‌کند و برای یک محیط تازه توصیه می‌شود.

## دانلود یا به‌روزرسانی دارایی‌های آفلاین

این فرمان فقط زمانی لازم است که پوشه دارایی‌ها ناقص باشد یا فهرست رسانه‌های Scratch تغییر کرده باشد؛ برای هر بیلد لازم نیست دوباره اجرا شود:

```powershell
npm run assets:download --workspace=packages/scratch-gui
```

## ساخت خروجی Android

از ریشه مخزن اجرا کنید:

```powershell
npm run build:android
```

خروجی در این مسیر ساخته می‌شود:

```text
packages/scratch-gui/build/
```

پس از هر تغییر کد، اجرای همین دستور برای ساخت نسخه جدید Android کافی است. تمام محتوای پوشه `build` را به پوشه assets برنامه Android منتقل کنید؛ انتقال تنها `gui.js` کافی نیست.

برای WebViewAssetLoader معمولاً صفحه با آدرسی مشابه زیر باز می‌شود:

```text
https://appassets.androidplatform.net/assets/build/index.html
```

نام دقیق مسیر باید با ساختار assets برنامه Android هماهنگ باشد.

## ساخت استاندارد همه پکیج‌ها

برای ساخت استاندارد monorepo و خروجی‌های انتشار همه workspaceها:

```powershell
npm run build
```

این فرمان با `build:android` متفاوت است و فایل‌های بیشتری برای بسته‌های npm و حالت‌های توسعه/standalone تولید می‌کند. برای کپی داخل برنامه Android از `npm run build:android` استفاده کنید.

## اجرای محیط توسعه

```powershell
npm start
```

یا به‌صورت صریح برای GUI:

```powershell
npm start --workspace=packages/scratch-gui
```

## پاک‌سازی خروجی‌ها

```powershell
npm run clean
```

## تست و بررسی

اجرای تمام تست‌های همه workspaceها:

```powershell
npm test
```

دستورهای متمرکز Scratch GUI:

```powershell
npm run test:unit --workspace=packages/scratch-gui
npm run test:lint --workspace=packages/scratch-gui
npm run build:verify-offline --workspace=packages/scratch-gui
```

فرمان `build:android` پس از webpack به‌صورت خودکار بررسی بهینه و آفلاین را نیز اجرا می‌کند. این بررسی وجود صفحه و bundle اصلی، فونت، bridge اندروید، دارایی‌های کتابخانه و نبود source map در خروجی بهینه را کنترل می‌کند.

## فایل‌های مهم سفارشی‌سازی

- `package.json`: میان‌بر ریشه برای `build:android`.
- `packages/scratch-gui/package.json`: فرمان‌های دانلود دارایی، بیلد Android و اعتبارسنجی آفلاین.
- `packages/scratch-gui/webpack.config.js`: تنظیمات خروجی Android، مسیرهای نسبی و حذف فایل‌های اضافی.
- `packages/scratch-gui/download-assets.js`: دریافت رسانه‌های لازم برای استفاده آفلاین.
- `packages/scratch-gui/scripts/verify-offline-build.js`: کنترل کامل‌بودن خروجی آفلاین.
- `packages/scratch-gui/src/lib/supported-locales.js`: زبان‌ها و پیام‌های محلی فارسی/عربی/انگلیسی.
- `packages/scratch-gui/src/lib/set-blocks-locale.js`: اعمال locale و اصلاح بلوک‌های مقایسه‌ای.
- `packages/scratch-gui/src/lib/android-number-input.js`: رفتار ورودی عددی در Android.
- `packages/scratch-gui/src/lib/libraries/translate-library-name.js`: ترجمه نام رسانه‌های پیش‌فرض.
- `packages/scratch-gui/src/lib/libraries/library-item-search.js`: جست‌وجوی فارسی و انگلیسی کتابخانه.
- `packages/scratch-gui/src/css/typography.css`: فونت و تایپوگرافی فارسی.
- `packages/scratch-gui/README.md`: قرارداد کامل‌تر ارتباط با WebView و نمونه‌های استفاده.

## نکات نگهداری

- پوشه `build` محصول تولیدشده است؛ آن را منبع تغییرات دستی قرار ندهید. پس از تغییر کد دوباره `npm run build:android` را اجرا کنید.
- source mapها عمداً در بیلد Android تولید نمی‌شوند. بیلدهای استاندارد یا توسعه ممکن است فایل‌های متفاوت و بزرگ‌تری بسازند.
- هنگام به‌روزرسانی از `upstream/develop`، تعارض‌های احتمالی در localeها، webpack، Scratch Blocks و کتابخانه رسانه‌ها باید با حفظ رفتارهای شرح‌داده‌شده در این سند حل شوند.
- پس از تغییر فهرست رسانه‌ها، ابتدا دارایی‌ها را دانلود و سپس بیلد Android را اجرا کنید تا verifier کمبود فایل را گزارش دهد.
- پیش از انتشار نسخه Android، حداقل بیلد Android، تست‌های واحد مرتبط و lint پکیج Scratch GUI را اجرا کنید.
